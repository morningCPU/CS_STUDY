使用条件判断
使用条件判断，可以让make根据运行时的不用情况选择不同的执行分支
libs_for_gcc = -lgnu
normal_libs = 

foo : $(objects)
ifeq ($(CC),gcc)
    $(CC) -o foo $(objects) $(libs_for_gcc)
else
    $(CC) -o foo $(objects) $(normal_libs)
endif

+ ifeq else endif
ifeq 表示条件语句的开始，并指定一个条件表达式
else 表示条件表达式为假的情况
endif 表示一个条件语句的结束
